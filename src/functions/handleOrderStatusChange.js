// Stub for handleOrderStatusChange — self-hosted mode
export async function handleOrderStatusChange(data) {
    console.log('[handleOrderStatusChange] Called with:', data);
    return { success: true, message: 'Order status change logged.' };
}
