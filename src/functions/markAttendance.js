// Stub for markAttendance — self-hosted mode
export async function markAttendance(data) {
    console.log('[markAttendance] Called with:', data);
    return { success: false, message: 'Auto attendance marking not supported. Please mark manually.' };
}
