// Stub: generateBudgetReportPDF - self-hosted mode
export async function generateBudgetReportPDF(data) {
  console.log('[generateBudgetReportPDF] Called:', JSON.stringify(data)?.substring(0,100));
  return { success: false, message: 'Function generateBudgetReportPDF not implemented in self-hosted mode.' };
}
export default generateBudgetReportPDF;
