// Stub for calculateMonthlyPayroll — self-hosted mode
export async function calculateMonthlyPayroll(data) {
    console.log('[calculateMonthlyPayroll] Called with:', data);
    return { success: false, message: 'Automated payroll calculation not available in self-hosted mode.' };
}
