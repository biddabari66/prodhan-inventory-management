// Stub: generateOrderReportPDF - self-hosted mode
export async function generateOrderReportPDF(data) {
  console.log('[generateOrderReportPDF] Called:', JSON.stringify(data)?.substring(0,100));
  return { success: false, message: 'Function generateOrderReportPDF not implemented in self-hosted mode.' };
}
export default generateOrderReportPDF;
