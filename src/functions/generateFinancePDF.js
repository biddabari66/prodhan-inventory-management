// Stub for generateFinancePDF — self-hosted mode
export async function generateFinancePDF(data) {
    console.log('[generateFinancePDF] PDF generation is stubbed in self-hosted mode', data);
    return { success: false, message: 'PDF generation not available. Please use browser print.' };
}
